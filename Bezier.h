#include "StdAfx.h"
#include "vecMath.h"

using namespace std;

class Bezier{
	int numberOfTangents;
		vector<vector<int>> Colors;
public:
	//Bezier ( void/*vector<vector<vecMath>> curves*/ );
	//~Bezier( void );
	void drawCurves(void);
	void getTangentColor(void);
private:
	
};

